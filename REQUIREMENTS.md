# 樱花动漫播放器 - 完整需求文档

## 1. 全局架构
- 4 个底部标签页：首页、发现、下载、我的
- 标签页切换 = 纯 CSS 显隐，不重建 DOM，不重新请求（首次加载后缓存数据）
- 浮层页面（搜索、详情、设置）覆盖在标签页之上

## 2. 首页
- 顶部搜索框，点击进入搜索浮层
- 二级 Tab：本地动漫 | 追番列表
- 本地动漫：2 列卡片网格，显示下载目录下的 IP 文件夹（封面+标题）
- 空状态：插画 + "还没有下载任何动漫~"
- 追番列表：2 列卡片网格，显示追番进度条和更新角标
- 点击本地卡片 → 递进浏览文件夹（IP → 季/篇 → mp4 文件列表）
- 点击追番卡片 → 进入详情页（在线模式）

## 3. 发现
- 分类标签：推荐、日本动漫、国产动漫、欧美动漫、动漫电影
- 切换分类：请求对应数据，更新网格
- 2 列卡片网格展示推荐/分类结果

## 4. 下载
- 显示下载队列：标题、集数、进度条、速度、ETA、状态
- 操作：暂停、恢复、取消
- 空状态："暂无下载任务"
- 下载任务轮询：有任务时 500ms，无任务时 3000ms

## 5. 我的
- 设置入口
- 刷新域名入口
- 关于信息

## 6. 搜索
- 双源搜索：本地文件夹名匹配 + 在线樱花动漫站点搜索
- 合并结果：本地命中排前面，右上角绿色"本地"标签
- 点击本地结果 → 本地文件浏览
- 点击在线结果 → 在线详情页（爬取动漫信息）
- 搜索结果去重（按 videoId）

## 7. 详情页（B 站风格）
- 上半：16:9 播放器区域
  - 初始显示封面图
  - 点击选集 → `<video>` 加载 m3u8 播放
  - 自定义控件：粉色进度条、播放/暂停、全屏按钮、锁定
  - 手势：左侧亮度、右侧音量、中间快进、双击暂停、长按 2x
  - HUD：音量/亮度浮动提示、快进时间预览
  - 点全屏按钮 → PlayerActivity 横屏全屏播放（完整 B 站手势）
- 下半可滚动内容：
  - 标题、标签、简介
  - 追番按钮（⭐ 追番 / ⭐ 已追番）
  - 批量下载按钮
  - 选集网格（4 列），当前播放集高亮
  - 点击选集 → 播放；长按 0.6s → 下载单集
- 多源切换：主源播放失败自动换备用源，用户无感知

## 8. 全屏播放器（PlayerActivity）
- WebView 加载 fullplayer.html
- 完整 B 站风格控件：粉色进度条 + 缓冲条 + 拖拽圆点
- 全套手势：左侧亮度、右侧音量、中间快进、长按 2x、双击暂停、锁定屏幕
- HUD：音量/亮度浮动条 + 百分比、快进时间预览、倍速提示
- 控件自动隐藏（3s），触摸唤醒
- 竖屏/横屏自适应布局
- 键盘快捷键：Space/←→/↑↓/M
- 点返回 → 退出全屏回到详情页

## 9. 下载引擎
- 默认下载路径：/storage/emulated/0/SakuraAnime/
- Android 11+ 申请 MANAGE_EXTERNAL_STORAGE 权限
- SAF 文件夹选择器可修改下载路径
- m3u8 下载：检测 master playlist → 递归解析子列表 → 获取 .ts 分片
- 无加密 m3u8：8 线程并发下载 .ts 分片，合并为 mp4
- 加密 m3u8：回退 ffmpeg 单线程下载
- 前台 Service 运行，通知栏显示进度
- 最多 2 个并发下载任务（Semaphore）

## 10. 本地文件管理
- 目录结构：根目录 > IP 文件夹 > 季/篇子文件夹 > mp4 文件
- 允许 IP 文件夹下直接放 mp4（跳过子文件夹层）
- 封面图：自动查找文件夹内的 jpg/png/cover.jpg
- 递进浏览：点击文件夹进入下一层，返回键返回上一层
- 点击 mp4 → 本地播放

## 11. 追番
- 详情页点击追番 → 加入数据库
- 追番列表：封面、标题、已看/总集数、更新红点
- 每次打开 App 检查更新（爬取详情页对比总集数）
- 有新集 → 红点提示
- 点击追番条目 → 进入详情页

## 12. 域名管理
- 内置域名列表：yinghua14.com, yhdmw2.com, 156yh.cc, hazardinfo.com, sqynyl.com
- 启动时逐一探测，缓存首个可用域名
- 请求失败自动切换到备用域名

## 13. 导航与返回
- navStack 追踪页面跳转
- 浮层（搜索/详情/设置）打开时推入栈，关闭时出栈
- 返回键处理：
  1. 本地文件浏览中 → 返回上级目录
  2. 浮层打开中 → 关闭浮层回到底层标签
  3. 非首页标签 → 回到首页
  4. 首页 → Toast "再按一次退出"，2s 内再按退出 App

## 14. 标签页状态保持
- 切换标签页不销毁 DOM，只做 CSS 显隐
- 每个标签页数据缓存到 JS 变量
- 首页：保留本地文件浏览位置
- 发现：保留分类选择和卡片列表
- 下载：保留任务列表（轮询更新）

## 15. UI 规范
- 主色调：#FB7299 B 站粉
- 底部标签栏：52px + safe-area-inset-bottom
- 卡片：2 列网格，16:10 封面，圆角 10px
- Toast：黑底白字，底部居中，2s 消失
- 空状态：居中图标 + 文字
- 亮色主题，白色背景 #F6F7F8

## 16. 权限
- Android 11+：MANAGE_EXTERNAL_STORAGE（跳转系统设置）
- Android 9-：WRITE/READ_EXTERNAL_STORAGE
- Android 13+：POST_NOTIFICATIONS
